# `StreamGetValue`

Check if key exists in stream.

## Arguments

| Argument | Description |
| -------- | ----------- |
| id       | Id          |
| key      | Key         |

## Return

`1` if key exists in stream, `0` otherwise.
